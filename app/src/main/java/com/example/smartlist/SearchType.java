package com.example.smartlist;

public enum SearchType {
    // Enum specifying whether the product the user chose was a specific item or a custom input.
    // Custom input: "mælk"
    // Specific: "Arla Letmælk"
    CUSTOM,
    SPECIFIC
}
